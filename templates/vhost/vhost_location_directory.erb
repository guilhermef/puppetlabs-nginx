  location <%= location %> {
<% if @location_cfg_prepend -%><% location_cfg_prepend.each do |key,value| -%>
    <%= key %> <%= value %>;
<% end -%><% end -%>
    root  <%= www_root %>;
    <% if try_files != :undef -%>
    try_files <%= Array(try_files).join(' ') %>;
    <% end -%>
    index <%= Array(index_files).join(' ') %>;
<% if @location_cfg_append -%><% location_cfg_append.each do |key,value| -%>
    <%= key %> <%= value %>;
<% end -%><% end -%>
  }
